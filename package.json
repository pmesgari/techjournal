{
	"name": "techjournal",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"prebuild": "node scripts/obsidian-callout-preprocess.js",
		"hugo": "npm run prebuild && hugo serve"
	},
	"devDependencies": {
		"wrangler": "^4.19.1"
	},
	"dependencies": {
		"glob": "^7.2.3"
	}
}
